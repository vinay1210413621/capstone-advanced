# AI Engineering Specification: CI/CD Workflows

<!-- TODO(new-service): this service should consume the platform's reusable workflows as-is (see
     .github/workflows/ci.yml and terraform.yml). Document here only service-specific inputs. -->

## 1. Workflow Inputs Used
## 2. Service-Specific Build Steps (if any, beyond npm ci / npm test)
## 3. Acceptance Criteria
